apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-ui-jose
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook-ui-jose
  template:
    metadata:
      labels:
        app: guestbook-ui-jose
    spec:
      containers:
      - image: gcr.io/heptio-images/ks-guestbook-demo:0.2
        name: guestbook-ui-jose
        ports:
        - containerPort: 80
